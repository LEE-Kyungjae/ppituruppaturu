# 삐뚜루빠뚜루 핵심 인프라 스택
# DB/Redis 공유 네트워크 - Blue/Green 모두가 참조하는 코어 서비스들

networks:
  ppituru-core:
    name: ppituru-core
    driver: bridge

services:
  postgres:
    image: postgres:15
    container_name: ppituru-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ppituru_dev_2024}
      POSTGRES_DB: ${POSTGRES_DB:-ppituru_db}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - ppituru-core
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: ppituru-redis
    networks:
      - ppituru-core
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redisdata:/data

volumes:
  pgdata:
    name: ppituru-pgdata
  redisdata:
    name: ppituru-redisdata
